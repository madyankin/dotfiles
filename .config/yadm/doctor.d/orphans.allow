# Config directories that are deliberately tracked without the app installed
# on this machine. One name per line.
#
# aerospace: tried on Artanis on 2026-09-23 and backed out the same day in
# favour of Rectangle. Kerrigan still runs it, and both hosts have their own
# variant of the toml, so the config stays tracked.
aerospace
