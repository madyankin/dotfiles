# Monospace font, shared by every theme. Matched to the iTerm2 default
# profile 'One': Normal Font = 'JetBrainsMonoNFM-Regular 15'
# (NFM = Nerd Font Mono). `dot font set <family>` rewrites this.
#
# Omarchy's Style > Font sets one family everywhere; this is the same idea, and
# the render pass fans it out to Ghostty, VS Code, Zed, iTerm2 and Terminal.app.
FONT_FAMILY="JetBrainsMono Nerd Font Mono"
FONT_SIZE=15
# UI font size where an app distinguishes it from the editor font.
FONT_UI_SIZE=13
